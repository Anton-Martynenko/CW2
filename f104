function stringSuffix(s) {
  let result = 0;
  for (let i = 0; i < s.length; i++){
    for (let j = 0; j < s.length; j++)
    if (s.slice(i)[j] === s[j]){result++}
    else {break}
  }
  return result;
}
